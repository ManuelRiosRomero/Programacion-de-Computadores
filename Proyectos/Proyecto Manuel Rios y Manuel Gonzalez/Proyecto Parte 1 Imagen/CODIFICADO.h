#ifndef CODIFICADO_H_INCLUDED
#define CODIFICADO_H_INCLUDED

void imprimir (char *cadena); //imprimir la cadena que se ingresa

char *descodificar(char *codificar, char *encryp,char *alpnormal); //coger la cadena codificada, y retornar una cadena descoficada

void imagen(char *cadenaFinal);


#endif // CODIFICADO_H_INCLUDED
